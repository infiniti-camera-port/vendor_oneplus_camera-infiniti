# vendor_oneplus_camera-infiniti

OnePlus 15 (infiniti) per-device camera vendor config: `camera-infiniti.mk` inheriting
`camera-sm8850-common.mk`. Carved (thin, history-preserving) from
`infiniti-camera-port/vendor_oplus_camera` @ `3a76ca3` — seeded by `opluscamera.mk`'s
rename history, reduced to the device inherit.

- Build path: `vendor/oneplus/camera-infiniti`
